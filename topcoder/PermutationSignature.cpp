#include <string>
#include <vector>
#include <set>

using namespace std; 

int lookahead( string & sig, int i )
{

}

class PermutationSignature {
	public:
	vector <int> reconstruct(string signature) {
		int start = signature[0] == 'I' ? 1 : 2;
		set<int> used;
		used.insert( start );

	}
};


// Powered by FileEdit
